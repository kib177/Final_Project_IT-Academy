spring:
  application:
   name: finalProject

  datasource:
    url: jdbc:postgresql://localhost:5432/test
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1998177

    spring:
      jpa:
        hibernate:
          ddl-auto=update


    application:
        name: finalProject

    server:
      port: 8080


